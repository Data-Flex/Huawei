#include <stdio.h>

int strchar (const char* cs, char c) {

    int i = 0;

    while (*(cs + i) != c && *(cs + i) != '\0') {

        i++;
        
        printf ("%c\n", *(cs + i));
    }

    return 0;
}

int main() {

    
}